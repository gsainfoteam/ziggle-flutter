include: package:flutter_lints/flutter.yaml

linter:
  rules:
    - directives_ordering
    - use_null_aware_elements

analyzer:
  enable-experiment:
    - null-aware-elements
  errors:
    invalid_annotation_target: ignore
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
